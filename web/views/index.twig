{% extends "layout.html" %}

{% block content %}
<p>Index ici</p>
<script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript">
      google.charts.load("current", {packages:["corechart"]});
      google.charts.setOnLoadCallback(drawChart);
      function drawChart() {
        var data = google.visualization.arrayToDataTable([
          ['area', 'number of testkit'],
          ['Europe',     3],
          ['America',      2],
          ['Asia',  5],
          ['Oceania', 1],
          ['Africa',    7]
        ]);

        var options = {
          title: 'Here is the repartition of the test-kits all around the world',
          pieHole: 0.4,
        };

        var chart = new google.visualization.PieChart(document.getElementById('donutchart'));
        chart.draw(data, options);
      }
    </script>
<div class="jumbotron text-center">
  <div class="container">
    <div id="donutchart" style="width: 900px; height: 500px;margin-left: auto;margin-right: auto;"></div>
	<h1> </h1>
  </div>
</div>
<div class="container">
  <div class="alert alert-info text-center" role="alert">
    You can see the first page by clicking<a href="Tdata.twig" class="alert-link">on this link</a>.
  </div>
  <hr>
  <div class="row">
   
  </div> <!-- row -->
   
</div>
<p>Et la</p>
{% endblock %}
